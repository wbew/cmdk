{
  "name": "cmdk",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build:ts && bun run build:assets",
    "build:ts": "bun build src/background.ts src/popup.ts src/content.ts --outdir dist --target browser",
    "build:assets": "cp manifest.json popup.html dist/ && cp -r icons dist/",
    "dev": "bun run build --watch"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.1.33",
    "typescript": "^5"
  }
}
